# Project-local Codex defaults. Loaded only when this repository is trusted.
model = "gpt-5.5"
model_reasoning_effort = "xhigh"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[features]
hooks = true

[sandbox_workspace_write]
network_access = false
